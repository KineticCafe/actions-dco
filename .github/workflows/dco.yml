name: DCO Check

on:
  pull_request:

jobs:
  local:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./
        with:
          exempt-authors: |
            @kineticcommerce.com
            @kineticcafe.com

  # remote:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: KineticCafe/actions-dco@v1.0
